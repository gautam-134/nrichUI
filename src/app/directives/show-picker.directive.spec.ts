import { ShowPickerDirective } from './show-picker.directive';

describe('ShowPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPickerDirective();
    expect(directive).toBeTruthy();
  });
});
