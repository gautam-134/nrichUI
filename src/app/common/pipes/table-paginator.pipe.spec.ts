import { TablePaginatorPipe } from './table-paginator.pipe';

describe('TablePaginatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TablePaginatorPipe();
    expect(pipe).toBeTruthy();
  });
});
