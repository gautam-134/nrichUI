import { ConvertSizeIntoReadableFormatPipe } from './convert-size-into-readable-format.pipe';

describe('ConvertSizeIntoReadableFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSizeIntoReadableFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
